{
  "version": 2,
  "revision": 0,
  "models": {
    "preset": "classic",
    "slots": {
      "governor": {
        "primary": { "profile": "deepseek_governor" },
        "fallbacks": [],
        "fallbackPolicy": { "enabled": false, "on": ["configuration", "capability", "connection", "rate_limit", "timeout", "provider_error", "invalid_response"] },
        "requirements": { "textInput": true, "streaming": true, "nativeToolCalling": true }
      },
      "coding": {
        "primary": { "profile": "glm_coding_worker" },
        "fallbacks": [],
        "fallbackPolicy": { "enabled": false, "on": ["configuration", "capability", "connection", "rate_limit", "timeout", "provider_error", "invalid_response"], "allowGovernorDirectFallback": true },
        "requirements": { "textInput": true, "structuredOutput": true }
      },
      "vision": {
        "primary": { "profile": "kimi_vision" },
        "fallbacks": [],
        "fallbackPolicy": { "enabled": false, "on": ["configuration", "capability", "connection", "rate_limit", "timeout", "provider_error", "invalid_response"], "allowGovernorDirectFallback": true },
        "requirements": { "textInput": true, "imageInput": true, "structuredOutput": true }
      }
    }
  },
  "defaults": { "permissionMode": "auto" },
  "git": { "protectedBranches": ["main", "master"], "worktreeRoots": [".deep-mix/worktrees"] },
  "codeIntelligence": { "defaultSemanticProvider": "local" }
}
