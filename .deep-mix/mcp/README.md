# MCP Boundary

`MCP` in Deep-Mix is reserved for external systems and real-time tools.

- Use `Skills` for reusable textual guidance.
- Use `Workflows` for deterministic sequencing.
- Use `MCP` for GitHub, browser, database, or other live tool integrations.

Server definitions live in `.deep-mix/mcp/servers.json`.
