# Workflows Boundary

`Workflows` are deterministic multi-step orchestrations. They define a fixed sequence of governor, worker, and tool actions.

Workflows are not repository rules. Repository rules stay in `AGENTS.md`.

Project workflows are discovered from `.deep-mix/workflows/*.workflow.json`.
